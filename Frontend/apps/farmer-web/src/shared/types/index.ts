/** Shared farmer domain types — re-export API contracts as needed. */
export type {
  Farm,
  TwinDto,
  PlanDto,
  Alert,
  WaterSource,
  SoilProfileRecord,
  FarmEconomicsResponse,
} from '@hv/api-types';
